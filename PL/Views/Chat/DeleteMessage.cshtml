@model PL.Models.ChatMessageModel

@{
    ViewBag.Title = "DeleteChatMessage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DeleteChatMessage</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ChatChatMessageDTO</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ChatMessage.Message)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ChatMessage.Message)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

	    <div class="form-actions no-color">
		    @Html.HiddenFor(m=>m.ChatId)
			@Html.HiddenFor(m=>m.ChatMessage.ID)
		    <input type="submit" value="Delete" class="btn btn-default"/> |
		    @Html.ActionLink("Back ", "OpenChat", new {id = Model.ChatId})
	    </div>
    }
</div>
